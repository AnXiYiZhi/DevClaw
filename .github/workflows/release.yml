name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  release:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-2022
          - os: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Sync version from tag
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          echo "Syncing version: $VERSION"
          python -c "
          from pathlib import Path
          import re, json

          version = '$VERSION'

          # src-tauri/Cargo.toml
          cargo = Path('src-tauri/Cargo.toml')
          cargo.write_text(
              re.sub(r'^version\s*=\s*\".*?\"', f'version = \"{version}\"', cargo.read_text(encoding='utf-8'), count=1, flags=re.MULTILINE),
              encoding='utf-8'
          )

          # package.json
          pkg = Path('package.json')
          data = json.loads(pkg.read_text(encoding='utf-8'))
          data['version'] = version
          pkg.write_text(json.dumps(data, indent=2, ensure_ascii=False) + '\n', encoding='utf-8')

          # src-tauri/tauri.conf.json
          conf = Path('src-tauri/tauri.conf.json')
          data = json.loads(conf.read_text(encoding='utf-8'))
          data['version'] = version
          conf.write_text(json.dumps(data, indent=2, ensure_ascii=False) + '\n', encoding='utf-8')
          "
          echo "Done. Verifying:"
          grep '^version' src-tauri/Cargo.toml
          python -c "import json; print(json.load(open('package.json'))['version'])"
          python -c "import json; print(json.load(open('src-tauri/tauri.conf.json'))['version'])"

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Add macOS targets
        if: runner.os == 'macOS'
        run: |
          rustup target add aarch64-apple-darwin x86_64-apple-darwin

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10.12.3
          run_install: false

      - name: Get pnpm store directory
        id: pnpm-store
        shell: bash
        run: echo "path=$(pnpm store path --silent)" >> $GITHUB_OUTPUT

      - name: Setup pnpm cache
        uses: actions/cache@v5
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-${{ runner.arch }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-${{ runner.arch }}-pnpm-store-

      - name: Install frontend deps
        run: pnpm install --frozen-lockfile

      - name: Prepare Tauri signing key
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}" ]; then
            echo "TAURI_SIGNING_PRIVATE_KEY is missing" >&2
            exit 1
          fi

          RAW="${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}"
          KEY_PATH="$RUNNER_TEMP/tauri_signing.key"

          if echo "$RAW" | head -n1 | grep -q '^untrusted comment:'; then
            printf '%s\n' "$RAW" > "$KEY_PATH"
          elif DECODED=$(printf '%s' "$RAW" | (base64 --decode 2>/dev/null || base64 -D 2>/dev/null)) \
            && echo "$DECODED" | head -n1 | grep -q '^untrusted comment:'; then
            printf '%s\n' "$DECODED" > "$KEY_PATH"
          elif echo "$RAW" | grep -Eq '^[A-Za-z0-9+/=]+$'; then
            ONE=$(printf '%s' "$RAW" | tr -d '\r\n')
            printf '%s\n%s\n' "untrusted comment: tauri signing key" "$ONE" > "$KEY_PATH"
          else
            echo "TAURI_SIGNING_PRIVATE_KEY format is not recognized" >&2
            exit 1
          fi

          KEY_B64=$(base64 < "$KEY_PATH" | tr -d '\r\n')
          if [ -z "$KEY_B64" ]; then
            echo "Failed to encode Tauri signing key" >&2
            exit 1
          fi

          echo "TAURI_SIGNING_PRIVATE_KEY=$KEY_B64" >> "$GITHUB_ENV"
          if [ -n "${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}" ]; then
            echo "TAURI_SIGNING_PRIVATE_KEY_PASSWORD=${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}" >> "$GITHUB_ENV"
          fi

      - name: Build Tauri App (macOS)
        if: runner.os == 'macOS'
        shell: bash
        timeout-minutes: 60
        env:
          APPLE_SIGNING_IDENTITY: "-"
        run: |
          set -euo pipefail
          pnpm tauri build --target universal-apple-darwin

      - name: Build Tauri App (Windows)
        if: runner.os == 'Windows'
        run: pnpm tauri build

      - name: Prepare macOS Assets
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p release-assets
          VERSION="${GITHUB_REF_NAME}"  # e.g., v3.5.0

          # Locate .app bundle
          APP_PATH=""
          for path in \
            "src-tauri/target/universal-apple-darwin/release/bundle/macos" \
            "src-tauri/target/aarch64-apple-darwin/release/bundle/macos" \
            "src-tauri/target/x86_64-apple-darwin/release/bundle/macos" \
            "src-tauri/target/release/bundle/macos"; do
            if [ -d "$path" ]; then
              [ -z "$APP_PATH" ] && APP_PATH=$(find "$path" -maxdepth 1 -name "*.app" -type d | head -1 || true)
            fi
          done

          if [ -z "$APP_PATH" ]; then
            echo "❌ No .app found" >&2
            exit 1
          fi

          TAR_GZ=""
          for path in \
            "src-tauri/target/universal-apple-darwin/release/bundle/macos" \
            "src-tauri/target/aarch64-apple-darwin/release/bundle/macos" \
            "src-tauri/target/x86_64-apple-darwin/release/bundle/macos" \
            "src-tauri/target/release/bundle/macos"; do
            if [ -d "$path" ]; then
              [ -z "$TAR_GZ" ] && TAR_GZ=$(find "$path" -maxdepth 1 -name "*.tar.gz" -type f | head -1 || true)
            fi
          done

          if [ -z "$TAR_GZ" ]; then
            echo "No macOS .tar.gz updater artifact found" >&2
            exit 1
          fi

          # 1) Collect .tar.gz and .sig for Tauri updater
          NEW_TAR_GZ="DevClaw-${VERSION}-macOS.tar.gz"
          cp "$TAR_GZ" "release-assets/$NEW_TAR_GZ"
          if [ -f "$TAR_GZ.sig" ]; then
            cp "$TAR_GZ.sig" "release-assets/$NEW_TAR_GZ.sig"
          else
            echo "No signature found for $TAR_GZ" >&2
            exit 1
          fi
          echo "macOS updater artifact copied: $NEW_TAR_GZ"

          # 2) Create styled DMG with create-dmg
          HOMEBREW_NO_AUTO_UPDATE=1 brew install create-dmg
          NEW_DMG="DevClaw-${VERSION}-macOS.dmg"
          DMG_STAGE_DIR="$RUNNER_TEMP/dmg-stage"
          rm -rf "$DMG_STAGE_DIR"
          mkdir -p "$DMG_STAGE_DIR"
          ditto "$APP_PATH" "$DMG_STAGE_DIR/DevClaw.app"

          create-dmg \
            --volname "DevClaw" \
            --background "src-tauri/icons/dmg-background.png" \
            --window-size 660 400 \
            --window-pos 200 120 \
            --icon-size 80 \
            --icon "DevClaw.app" 180 220 \
            --hide-extension "DevClaw.app" \
            --app-drop-link 480 220 \
            --no-internet-enable \
            "release-assets/$NEW_DMG" \
            "$DMG_STAGE_DIR"

          rm -rf "$DMG_STAGE_DIR"
          echo "✅ Styled DMG created: $NEW_DMG"

      - name: Prepare Windows Assets
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          New-Item -ItemType Directory -Force -Path release-assets | Out-Null
          $VERSION = $env:GITHUB_REF_NAME  # e.g., v3.5.0
          # 仅打包 MSI 安装器 + .sig（用于 Updater）
          $msi = Get-ChildItem -Path 'src-tauri/target/release/bundle/msi' -Recurse -Include *.msi -ErrorAction SilentlyContinue | Select-Object -First 1
          if ($null -eq $msi) {
            # 兜底：全局搜索 .msi
            $msi = Get-ChildItem -Path 'src-tauri/target/release/bundle' -Recurse -Include *.msi -ErrorAction SilentlyContinue | Select-Object -First 1
          }
          if ($null -ne $msi) {
            $dest = "DevClaw-$VERSION-Windows.msi"
            Copy-Item $msi.FullName (Join-Path release-assets $dest)
            Write-Host "Installer copied: $dest"
            $sigPath = "$($msi.FullName).sig"
            if (Test-Path $sigPath) {
              Copy-Item $sigPath (Join-Path release-assets ("$dest.sig"))
              Write-Host "Signature copied: $dest.sig"
            } else {
              throw "Signature not found for $($msi.Name)"
            }
          } else {
            Write-Warning 'No Windows MSI installer found'
          }
          # 绿色版（portable）：仅可执行文件打 zip（不参与 Updater）
          $exeCandidates = @(
            'src-tauri/target/release/devclaw.exe',
            'src-tauri/target/x86_64-pc-windows-msvc/release/devclaw.exe'
          )
          $exePath = $exeCandidates | Where-Object { Test-Path $_ } | Select-Object -First 1
          if ($null -ne $exePath) {
            $portableDir = 'release-assets/DevClaw-Portable'
            New-Item -ItemType Directory -Force -Path $portableDir | Out-Null
            Copy-Item $exePath $portableDir
            $portableIniPath = Join-Path $portableDir 'portable.ini'
            $portableContent = @(
              '# DevClaw portable build marker',
              'portable=true'
            )
            $portableContent | Set-Content -Path $portableIniPath -Encoding UTF8
            $portableZip = "release-assets/DevClaw-$VERSION-Windows-Portable.zip"
            Compress-Archive -Path "$portableDir/*" -DestinationPath $portableZip -Force
            Remove-Item -Recurse -Force $portableDir
            Write-Host "Windows portable zip created: DevClaw-$VERSION-Windows-Portable.zip"
          } else {
            Write-Warning 'Portable exe not found'
          }

      - name: List prepared assets
        shell: bash
        run: |
          ls -la release-assets || true

      - name: Collect Signatures
        shell: bash
        run: |
          set -euo pipefail
          echo "Collected signatures (if any alongside artifacts):"
          ls -la release-assets/*.sig || echo "No signatures found"

      - name: Upload release artifacts to workflow
        uses: actions/upload-artifact@v7
        with:
          name: release-assets-${{ runner.os }}-${{ matrix.arch || runner.arch }}
          path: release-assets/*
          if-no-files-found: error

      - name: List generated bundles (debug)
        if: always()
        shell: bash
        run: |
          echo "Listing bundles in src-tauri/target..."
          find src-tauri/target -maxdepth 4 -type f -name "*.*" 2>/dev/null || true

  publish-release:
    name: Publish GitHub Release
    runs-on: ubuntu-22.04
    needs: release
    permissions:
      contents: write
    steps:
      - name: Download built release artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: release-assets-*
          path: release-assets
          merge-multiple: true

      - name: List downloaded release artifacts
        shell: bash
        run: |
          set -euo pipefail
          ls -la release-assets

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.ref_name }}
          name: DevClaw ${{ github.ref_name }}
          prerelease: false
          body: |
            ## DevClaw ${{ github.ref_name }}

            🌐 **Only Official Website / 唯一官方网站 / 唯一の公式サイト**: [devclaw.cc.cd](https://devclaw.cc.cd)

            API编程工具统一管理客户端 提供环境一键安装 供应商任意切换

            ### 下载

            - **macOS**: `DevClaw-${{ github.ref_name }}-macOS.dmg`
            - **Windows**: `DevClaw-${{ github.ref_name }}-Windows.msi`（安装版）或 `DevClaw-${{ github.ref_name }}-Windows-Portable.zip`（绿色版）
          files: release-assets/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  assemble-latest-json:
    name: Assemble latest.json
    runs-on: ubuntu-22.04
    needs: publish-release
    permissions:
      contents: write
    steps:
      - name: Prepare GH
        run: |
          gh --version || (type -p curl >/dev/null && sudo apt-get update && sudo apt-get install -y gh || true)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Download all release assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euxo pipefail
          TAG="${GITHUB_REF_NAME}"
          mkdir -p dl
          gh release download "$TAG" --dir dl --repo "$GITHUB_REPOSITORY"
          ls -la dl || true
      - name: Generate latest.json
        env:
          REPO: ${{ github.repository }}
          TAG: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          VERSION="${TAG#v}"
          PUB_DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          base_url="https://github.com/$REPO/releases/download/$TAG"
          # 初始化空平台映射
          mac_url=""; mac_sig=""
          win_url=""; win_sig=""
          shopt -s nullglob
          for sig in dl/*.sig; do
            base=${sig%.sig}
            fname=$(basename "$base")
            url="$base_url/$fname"
            sig_content=$(cat "$sig")
            case "$fname" in
              *.tar.gz)
                # 视为 macOS updater artifact
                mac_url="$url"; mac_sig="$sig_content";;
              *.msi|*.exe)
                win_url="$url"; win_sig="$sig_content";;
            esac
          done
          # 构造 JSON（仅包含存在的目标）
          tmp_json=$(mktemp)
          {
            echo '{'
            echo "  \"version\": \"$VERSION\",";
            echo "  \"notes\": \"Release $TAG\",";
            echo "  \"pub_date\": \"$PUB_DATE\",";
            echo '  "platforms": {'
            first=1
            if [ -n "$mac_url" ] && [ -n "$mac_sig" ]; then
              for key in darwin-aarch64 darwin-x86_64; do
                [ $first -eq 0 ] && echo ','
                echo "    \"$key\": {\"signature\": \"$mac_sig\", \"url\": \"$mac_url\"}"
                first=0
              done
            fi
            if [ -n "$win_url" ] && [ -n "$win_sig" ]; then
              [ $first -eq 0 ] && echo ','
              echo "    \"windows-x86_64\": {\"signature\": \"$win_sig\", \"url\": \"$win_url\"}"
              first=0
            fi
            echo '  }'
            echo '}'
          } > "$tmp_json"
          echo "Generated latest.json:" && cat "$tmp_json"
          mv "$tmp_json" latest.json
      - name: Upload latest.json to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euxo pipefail
          gh release upload "$GITHUB_REF_NAME" latest.json --clobber --repo "$GITHUB_REPOSITORY"

  publish-gitee:
    name: Publish to Gitee
    runs-on: ubuntu-22.04
    needs: publish-release
    env:
      GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
      GITEE_REPO: ${{ secrets.GITEE_REPO }}
    steps:
      - name: Download zip and dmg only
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euxo pipefail
          TAG="${GITHUB_REF_NAME}"
          mkdir -p dl
          gh release download "$TAG" --dir dl --repo "$GITHUB_REPOSITORY" \
            --pattern "*Portable*.zip" --pattern "*macOS.dmg"
          ls -la dl

      - name: Create or reuse Gitee release
        id: create_release
        run: |
          set -euxo pipefail
          TAG="${GITHUB_REF_NAME}"

          # Check the tag directly so a partial previous run can be resumed reliably.
          LOOKUP_CODE=$(curl -sS --retry 2 --retry-delay 5 --retry-all-errors --connect-timeout 20 --max-time 120 -w "%{http_code}" -o /tmp/gitee_release_by_tag.json \
            "https://gitee.com/api/v5/repos/${GITEE_REPO}/releases/tags/${TAG}?access_token=${GITEE_TOKEN}")

          if [ "$LOOKUP_CODE" = "200" ]; then
            RELEASE_ID=$(python3 -c "import json; print(json.load(open('/tmp/gitee_release_by_tag.json'))['id'])")
            echo "Gitee release already exists (id=${RELEASE_ID}), reusing"
            echo "release_id=${RELEASE_ID}" >> $GITHUB_OUTPUT
            exit 0
          fi

          if [ "$LOOKUP_CODE" != "404" ]; then
            echo "::error::Gitee release lookup failed with HTTP ${LOOKUP_CODE}"
            cat /tmp/gitee_release_by_tag.json || true
            exit 1
          fi

          # Create release — access_token must be a query parameter, NOT inside JSON body
          # target_commitish is required by Gitee API (specify the branch name)
          HTTP_CODE=$(curl -sS --retry 2 --retry-delay 5 --retry-all-errors --connect-timeout 20 --max-time 120 -w "%{http_code}" -o /tmp/gitee_response.json \
            -X POST "https://gitee.com/api/v5/repos/${GITEE_REPO}/releases?access_token=${GITEE_TOKEN}" \
            -H "Content-Type: application/json" \
            -d "{
              \"tag_name\": \"${TAG}\",
              \"target_commitish\": \"master\",
              \"name\": \"DevClaw ${TAG}\",
              \"body\": \"Release ${TAG}\",
              \"prerelease\": false
            }")

          echo "HTTP status: ${HTTP_CODE}"
          echo "Response body:"
          cat /tmp/gitee_response.json

          if [ "${HTTP_CODE}" -lt 200 ] || [ "${HTTP_CODE}" -ge 300 ]; then
            echo "::error::Gitee create release failed with HTTP ${HTTP_CODE}"
            exit 1
          fi

          RELEASE_ID=$(python3 -c "import json; print(json.load(open('/tmp/gitee_response.json'))['id'])")
          echo "release_id=${RELEASE_ID}" >> $GITHUB_OUTPUT
          echo "Gitee release created: ${RELEASE_ID}"

      - name: Upload assets to Gitee
        run: |
          set -euxo pipefail
          RELEASE_ID="${{ steps.create_release.outputs.release_id }}"
          MAX_RETRIES=1
          FAILED_ASSETS=0

          for f in dl/*; do
            [ -f "$f" ] || continue
            FILENAME=$(basename "$f")

            # Check if file already uploaded (from a previous partial run)
            EXISTING=$(curl -sS --retry 2 --retry-delay 5 --retry-all-errors --connect-timeout 20 --max-time 120 "https://gitee.com/api/v5/repos/${GITEE_REPO}/releases/${RELEASE_ID}/attach_files?access_token=${GITEE_TOKEN}&page=1&per_page=100" \
              | python3 -c "
          import sys, json
          for f in json.load(sys.stdin):
              if f.get('name') == '${FILENAME}':
                  print('yes')
                  sys.exit(0)
          " || true)

            if [ "$EXISTING" = "yes" ]; then
              echo "⏭ ${FILENAME} already uploaded, skipping"
              continue
            fi

            for attempt in $(seq 1 $MAX_RETRIES); do
              echo "Uploading ${FILENAME} (attempt ${attempt}/${MAX_RETRIES})..."

              set +e
              HTTP_CODE=$(curl -sS --connect-timeout 20 --max-time 300 -w "%{http_code}" -o /tmp/gitee_upload_response.json \
                -X POST "https://gitee.com/api/v5/repos/${GITEE_REPO}/releases/${RELEASE_ID}/attach_files?access_token=${GITEE_TOKEN}" \
                -H "Content-Type: multipart/form-data" \
                -F "file=@${f}")
              CURL_STATUS=$?
              set -e

              echo "curl status: ${CURL_STATUS}"
              echo "HTTP status: ${HTTP_CODE}"
              cat /tmp/gitee_upload_response.json || true

              if [ "${CURL_STATUS}" -eq 0 ] && [ "${HTTP_CODE}" -ge 200 ] && [ "${HTTP_CODE}" -lt 300 ]; then
                DOWNLOAD_URL=$(python3 -c "import json; print(json.load(open('/tmp/gitee_upload_response.json')).get('browser_download_url','N/A'))")
                echo "✅ Uploaded: ${DOWNLOAD_URL}"
                break
              fi

              if [ "$attempt" -eq "$MAX_RETRIES" ]; then
                echo "::warning::Failed to upload ${FILENAME} to Gitee after ${MAX_RETRIES} attempts (curl ${CURL_STATUS}, HTTP ${HTTP_CODE})"
                FAILED_ASSETS=1
                break
              fi

              echo "⚠️ Upload failed, retrying in 10s..."
              sleep 10
            done
          done
          if [ "${FAILED_ASSETS}" -ne 0 ]; then
            echo "::warning::Some Gitee mirror assets were not uploaded; GitHub release and updater artifacts remain available."
          else
            echo "All assets uploaded to Gitee"
          fi

  upload-to-worker:
    name: Upload to DevClawWorker
    runs-on: ubuntu-22.04
    needs: publish-release
    steps:
      - name: Download release assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euxo pipefail
          TAG="${GITHUB_REF_NAME}"
          mkdir -p dl
          gh release download "$TAG" --dir dl --repo "$GITHUB_REPOSITORY" \
            --pattern "*Portable*" --pattern "*.dmg"
          ls -la dl

      - name: Upload Windows Portable ZIP
        run: |
          set -euxo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          ZIP=$(find dl -name "*Portable*.zip" | head -1)
          if [ -z "$ZIP" ]; then
            echo "No portable zip found, skipping"
            exit 0
          fi
          echo "Uploading $ZIP as version $VERSION..."
          curl -f -X POST "https://devclaw.ccwu.cc/api/upload/windows?version=${VERSION}" \
            -H "Authorization: Bearer ${{ secrets.DEVCLAWWORKER_UPLOAD_TOKEN }}" \
            -H "Content-Type: application/zip" \
            --data-binary "@${ZIP}"
          echo "Windows upload done"

      - name: Upload macOS DMG
        run: |
          set -euxo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          DMG=$(find dl -name "*.dmg" | head -1)
          if [ -z "$DMG" ]; then
            echo "No DMG found, skipping"
            exit 0
          fi
          echo "Uploading $DMG as version $VERSION..."
          curl -f -X POST "https://devclaw.ccwu.cc/api/upload/macos?version=${VERSION}" \
            -H "Authorization: Bearer ${{ secrets.DEVCLAWWORKER_UPLOAD_TOKEN }}" \
            -H "Content-Type: application/octet-stream" \
            --data-binary "@${DMG}"
          echo "macOS upload done"
